# Database files
db.sqlite3
db.sqlite3-shm
db.sqlite3-wal

# Environment files
.env

# Log files
logs/*.log

# Binary
newsfornerds

# Service files (local)
*.service

# Build scripts (local)
doBuild.sh
doRestart.sh

# Local test helpers (not part of the shipped binary)
cmd/flippublic/
flippublic
flippublic.exe

# Ad-hoc debug scripts (kept locally for re-running with
# the integration suite; not part of the shipped binary)
dbg_*.py

# Integration-test screenshots (regenerated by integration_tests.py)
screenshots/
